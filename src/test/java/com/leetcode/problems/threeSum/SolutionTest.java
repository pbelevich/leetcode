package com.leetcode.problems.threeSum;

import org.hamcrest.Matcher;
import org.junit.Test;

import java.util.Arrays;
import java.util.List;

import static org.hamcrest.Matchers.*;
import static org.junit.Assert.*;

public class SolutionTest {

    public static final Solution SOLUTION = new Solution();

    @Test
    public void testThreeSum() throws Exception {
//        Matcher<Iterable<?>> iterableMatcher = iterableWithSize(1);
//        Matcher<Iterable<? extends List<Integer>>> contains = contains(Arrays.asList(0, 0, 0));
//        assertThat(SOLUTION.threeSum(new int[] {0, 0, 0}), allOf(iterableMatcher, contains));
    }

}